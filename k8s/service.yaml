apiVersion: v1
kind: Service
metadata:
  labels:
    app: tekton-node
  name: tekton-node
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
  selector:
    app: tekton-node
